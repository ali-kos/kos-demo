const model = {
  namespace: "form-info",
  initial: {
    baseForm: {
      name: "Augustus",
      age: 33
    }
  },
  reducers: {},
  async setup(dispatch, getState, action) {}
};

export default model;
